# SESSION — 2026-07-28 (a)

**Project:** SMB Q&A Agent
**Phase:** Phase 0 — Validate
**Agent/model:** Claude Code (Sonnet 5)
**Started from:** sessions/2026-07-27-a.md's next-step note (confirm owner's outstanding console/billing actions, live test) — session actually opened on a different thread first (see below)

---

## Intent
Resolve how to put the prototype's UI in front of the pilot partner's real clients (a discussion that started in a prior session but was never written down before the window closed, so effectively restarted from scratch), decide and execute a hosting path, and stand up a parallel comparison experiment using Manus.

## Work log
- Session opened in a different directory (`build-standards 2`) with a mismatched `CLAUDE.md` describing a different, non-existent system layout; located the actual project at `standards/projects/smb-qa-agent` and re-booted correctly via `core/AGENTS.md`'s sequence (`CONDUCT-core.md` → `build/CONDUCT-delta.md` → `STATE.md` → latest session log → `build/INDEX.md`).
- Confirmed with owner: the two outstanding safety actions (Anthropic console spend cap, billing alerts) and the live end-to-end test from the prior session are still not done.
- Owner raised "how do we put this in front of the client" — no decision file or STATE.md entry existed for this from the prior session, so per CONDUCT-core §6 it counted as not having happened; restarted the thread properly.
- Reviewed `src/server.js` and confirmed it uses in-memory session/daily-counter state and local-disk writes for `client-chats/` and `usage.log` — ruled out Vercel serverless as-is (stateless functions would silently break the daily cost cap and lose the handoff files) without a real refactor onto Supabase Postgres, which was considered and set aside for this disposable-test round.
- Researched Render vs. Railway vs. Fly.io (mix of primary docs via WebFetch and general web search, the latter mostly low-authority comparison sites, flagged as such). Confirmed via Render's own docs: Starter tier (512MB/0.5CPU) doesn't sleep, persistent disks available on paid tiers and preserve their mount path across deploys.
- Owner asked about Manus (has seen projects served via manus.im). Researched with appropriate skepticism given thin/inconsistent sourcing; concluded Manus's fit for *deploying our existing code* was uncertain, but reframed per owner's actual proposal: let Manus **build its own equivalent app** rather than host ours, run as a parallel comparison track — legitimate given the prototype was never meant to be permanent (Phase 2 re-decides the real stack if this continues).
- Wrote `ops/manus-delivery-brief.md` first (deploy-existing-code framing), then superseded it with `ops/manus-build-prd.md` once the framing shifted to build-from-scratch — the PRD carries the decisions/002-003 safety floor (access gate, rate/session/daily caps, cost-math methodology, handoff-artifact behavior) as hard requirements, since a from-scratch build won't automatically inherit engineering that isn't visible from outside the app.
- Decided hosting for the existing Node/Express build: Render, Starter + persistent disk. Wrote `decisions/004-hosting-render.md` with the full options table and an explicit UNCHECKED flag on the exact disk $/GB price (Render's pricing page didn't yield it via automated fetch).
- Made the corresponding code change: `src/server.js` now reads a `DATA_DIR` env var (defaults to project root locally) instead of hardcoding `client-chats/`/`usage.log` paths relative to `__dirname` — needed so the persistent disk mount actually gets used. Updated `.env.example` and `README.md` to match.
- Owner has Vercel + Supabase accounts (saved to cross-session memory, not project-specific — relevant to any future provider decision, and specifically to Phase 2 if this project continues).
- Set up git: `git init -b main`, added `.DS_Store` to `.gitignore`, staged and committed all project files except secrets/generated artifacts (`.env`, `usage.log`, `client-chats/`, `node_modules/` already excluded), created `dev` as a local integration branch. Owner created a private GitHub repo (`github.com/Daa7314/smb-qa-agent`, no README/gitignore/license initialized); pushed both `main` and `dev`.
- Clarified for owner: the push used the existing `osxkeychain` git credential helper already configured on their machine — no credentials were ever seen or handled directly.
- Walked owner through Render service creation step by step (per explicit request to slow down — see feedback memory saved this session): GitHub App access scoped to select-repos-only (not all repos), repo selected, Starter instance, `ANTHROPIC_API_KEY` and `ACCESS_CODE` env vars added, service created and reported live by owner. Added persistent disk at mount path `/var/data`, then added `DATA_DIR=/var/data` env var — redeploy should trigger automatically but not yet confirmed.
- Owner separately asked for a full repo vulnerability review + cleanup recommendations — explicitly deferred to later; logged in `STATE.md` under a new "Outstanding — for later" section with a note to load `3-backend-security` + `security-review` when it's picked up.

## Checked
- Local git commands (`init`, `add`, `commit`, `branch`, `remote add`, `push`) — actual command output confirmed success for each step, including both `main` and `dev` showing up on the new GitHub remote.
- Render's Starter instance specs (512MB/0.5CPU) and persistent-disk-survives-deploys behavior — confirmed via Render's own docs (`docs/compute-plans`, `docs/disks`), not assumed.
- `.gitignore` correctly excludes `.env`, `usage.log`, `client-chats/`, `node_modules/`, `.DS_Store` before the initial commit — verified via `git status --porcelain` showing no secrets staged.

## Unchecked / assumed
- Whether the Render service is actually reachable and functioning end-to-end — owner reports "link is live," not independently verified by fetching it.
- Whether the `DATA_DIR=/var/data` change has actually redeployed and taken effect, and whether `client-chats/`/`usage.log` are now genuinely persisting across restarts on the real disk — not yet tested.
- The exact Render persistent-disk $/GB price — still unconfirmed against a primary source (flagged in decisions/004).
- No live end-to-end test with the real Anthropic key has been run yet (carried over from the prior session, still outstanding).
- Anthropic console spend cap and billing alerts — owner confirmed these are still not done as of this session.
- Real business docs have not yet been pasted into `src/docs/business-docs.txt`.
- Manus has not yet actually received `ops/manus-build-prd.md` — no output to evaluate yet.

## Decisions made this session
- Hosting for the existing prototype: Render, Starter + persistent disk → `decisions/004-hosting-render.md`

## Scope changes flagged
- Manus track reframed mid-session from "deploy our existing code" to "build its own equivalent app," at the owner's direction — not silently absorbed, discussed explicitly and logged in `STATE.md` and superseded `ops/manus-delivery-brief.md` with `ops/manus-build-prd.md`.

## Half-finished / open right now
- Render deploy: service live per owner, but disk + `DATA_DIR` just added — redeploy completion and actual persistence behavior not yet confirmed.
- Live end-to-end test with the real API key: still not done (this is the next concrete step once the redeploy is confirmed).
- Owner's two console-side safety actions (spend cap, billing alerts): still outstanding.
- Real business docs: still placeholder content in `src/docs/business-docs.txt`.
- Manus build track: PRD written, not yet handed to Manus, no output to compare against the Render track yet.
- Full repo vulnerability review: explicitly deferred by the owner, logged for later, not started.

---

## Session close
**STATE.md updated:** yes — added the two-parallel-hosting-tracks section, the Manus PRD reframing note, and the "Outstanding — for later" security-review item.
**Next session should start by:** confirming the Render redeploy completed and the service is actually reachable; running one live end-to-end test with the real key and real docs (once docs are pasted); checking whether the owner has done the Anthropic console spend cap and billing alerts; and checking whether Manus has been given the PRD yet and, if so, reviewing its output against the PRD's 10 hard requirements before anything goes near the pilot partner's real clients.
