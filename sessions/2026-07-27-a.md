# SESSION — 2026-07-27 (a)

**Project:** SMB Q&A Agent
**Phase:** Phase 0 — Validate
**Agent/model:** Claude Code (Sonnet 5)
**Started from:** brand-new project, no prior STATE.md

---

## Intent
Stand up the project workspace and run the Phase 0 validation interview: problem/person, evidence, alternatives, cheapest test, success criteria, opportunity cost — before any scoping or architecture work begins.

## Work log
- Owner described the idea: multi-tenant Q&A agent for small businesses — owner uploads docs (pricelist, services), an LLM/agent answers client questions grounded in those docs, optionally hands off or books depending on workflow complexity. Framed as an "800/mybusinessagent"-type solution for businesses without a tech team or bandwidth.
- Ran the Phase 0 "ask before acting" questions:
- (Continuing same session, later same day) Owner confirmed "go for it" on the prototype track from decisions/001. Clarified terminology confusion: "concierge test" is the standards system's generic term for manual-fulfillment testing, unrelated to the other, differently-named project `projects/concierge` in this workspace.
- Recommended stack for the bare-bones prototype (Node/Express, Claude Haiku 4.5, docs stuffed into a cached system prompt, shared-access-code gate) — owner confirmed. Set monthly budget ceiling at $20 and confirmed an existing Anthropic API account. Wrote decisions/002 with the cost math behind the safeguards.
- Mid-build, owner requested a handoff mechanism: after each chat, a summary + full transcript for the business owner. Checked `5-data-lifecycle` (jump gate, applicable any phase) given this stores client-typed content. Wrote decisions/003 covering the design, the revised cost math (one extra LLM call per session shifted the daily cap down from 75 to 45/day to stay under the $20 ceiling), and `ops/data.md` for the lightweight data inventory/retention.
- Built the prototype: `src/server.js` (Express server — access-code gate, per-IP rate limiting, per-session message cap, idle timeout, daily hard cap, truncated conversation context, cached grounding via Anthropic prompt caching, per-session handoff file generation), `public/index.html` + `public/chat.js` (access-code gate UI, chat UI, end-chat button, all rendering via `textContent` only), `src/docs/business-docs.txt` (placeholder for the partner's real content), `.env.example`, `.gitignore`, `README.md`, `ops/costs.md`, `ops/data.md`.
- Ran `npm install` — clean, 0 vulnerabilities.
- Smoke-tested locally with a placeholder (non-functional) API key via curl:
  - **Person/problem:** service-business owners who currently answer client Q&A manually; other verticals exist but are not the focus of this round.
  - **Evidence:** one committed POC partner willing to run a real test — classified as conversations/commitment, N=1, not yet evidence of broad demand.
  - **Current alternative:** manual chat walkthroughs of pricing/services — observed to be slow and non-scaling. This is real observed cost, not assumption.
  - **Cheapest test:** owner initially proposed prototype-only (rung 4). Pushed back per skill standard (building at rung 4 to answer a rung 1 question). Owner settled on concierge test (rung 3) run in parallel with a bare-bones prototype build, given the committed partner and a fixed runway.
  - **Success criteria:** ≥5 business owners OTHER than the POC partner request a similar solution, within 6-8 weeks of the concierge/prototype starting. Tightened from an initial vague "5 requests for similar" — clarified as outside owners, not the partner's own clients or team.
  - **Opportunity cost / runway:** owner will sustain this for 6-8 weeks before needing a signal — matches the test window above.
- Created project workspace: STATE.md, sessions/, decisions/, handoffs/.
- Wrote decisions/001-phase0-test-design.md recording the test design and the honest tradeoffs considered.
- Project given a placeholder slug, "smb-qa-agent" — owner has not chosen a name yet.

## Checked
- Server starts cleanly with required env vars present (`npm start` → listens on configured port, logs configured caps) — confirmed via terminal output.
- Static page serves (`GET /` → 200).
- Wrong access code rejected (`POST /api/access` with bad code → 401).
- Chat rejected with no valid session (`POST /api/chat` with no cookie → 440).
- Correct access code grants a session cookie (`POST /api/access` with right code → 200, cookie set).
- Chat endpoint degrades gracefully on an Anthropic API failure (tested with a placeholder/invalid key → 502 with a plain-language message, no crash, error logged server-side).
- Handoff file generation runs even when the summary call fails: `client-chats/*.md` was written with the fallback placeholder text plus the full raw transcript, confirming the "owner never loses the log even if the summary breaks" design goal from decisions/003.
- Per-IP rate limiter fires and persists across sessions for the same IP (confirmed when a later test batch was immediately blocked with 429, carried over from an earlier batch within the same 15-minute window — same-IP scoping confirmed working).
- Daily cap counter increments correctly (`usage.log` showed `daily_count=1/45` after one chat attempt).
- All smoke-test artifacts (`.env` with placeholder key, `usage.log`, `client-chats/` test transcript, `/tmp` scratch files, and a test line appended to `business-docs.txt`) removed after testing — workspace confirmed clean via `ls -la`.

## Unchecked / assumed
- Whether the POC partner's clients will actually engage differently with concierge vs. prototype answers — this is exactly what the test window is designed to find out, not yet known.
- "Multi-tenant" and "workflow complexity" (handoff vs. booking) are the owner's framing of the eventual product, not yet scoped — Phase 1 will need to pin down what v1 actually covers.
- Never tested against a real Anthropic API key — the happy path (actual grounded answer returned, actual LLM-generated summary produced) is UNCHECKED against a live key. Owner should do one real end-to-end run before sharing the link with the POC partner's clients.
- Owner's two outstanding actions from `ops/costs.md` (provider-side hard spend cap, billing alerts at $10/$16) are not yet confirmed done — code-level limits alone are not sufficient per the cost-control standard.

## Decisions made this session
- Phase 0 test design (concierge + parallel prototype, single vertical/tenant, 6-8 week window, ≥5-outside-owner success criterion) → decisions/001-phase0-test-design.md
- Prototype stack and spend safeguards (Node/Express, Claude Haiku 4.5, cached-context grounding, access code + rate limit + session/daily caps, $20/month ceiling) → decisions/002-prototype-stack-and-safeguards.md
- Per-session handoff summary + transcript for the business owner, and the resulting daily-cap revision (75 → 45) → decisions/003-session-handoff-log.md

## Scope changes flagged
- Owner requested a client-chat handoff mechanism (summary + full transcript per session) mid-build. Not in the original decisions/001 test design — raised by the owner directly, addressed in decisions/003, not silently absorbed.

## Half-finished / open right now
- The Phase 0 test itself has not run yet — it's a 6-8 week real-world test (concierge Q&A + parallel prototype build with the POC partner). This session built the prototype vehicle; it did not execute or resolve the actual test.
- No KILL/PIVOT/CONTINUE decision exists yet — that gets written when the test resolves, per the skill's exit protocol. Do not proceed to Phase 1 scoping before that decision is recorded and signed off.
- Prototype has never been run against a real Anthropic key or real client traffic — owner should do one live end-to-end check before sharing the access code with the POC partner's clients.
- Owner still needs to: set a hard spend cap on the Anthropic console, set billing alerts at $10/$16, and paste the partner's real docs into `src/docs/business-docs.txt` — none of this runs usefully until those are done.

---

## Session close
**STATE.md updated:** yes — Phase 0 in progress; prototype built and smoke-tested; standing notes updated with prototype location and outstanding owner actions.
**Next session should start by:** confirming the owner has done the two outstanding console/billing steps and run one live test with real docs; then checking in periodically on the concierge test and prototype's real-world usage. Once the 6-8 week window closes (or a clear KILL/PIVOT/CONTINUE signal arrives earlier), record the decision in decisions/004-... and, if CONTINUE, write the Phase 0→1 handoff naming the riskiest surviving assumption for Scope.
